package com.example.androidassignment.utils

import androidx.lifecycle.LiveData
import com.example.androidassignment.data.User
import com.example.androidassignment.model.UserInfo

//TODO - cast to generics <T> while recieve and while return
class ConvertLiveDataListToUserInfo {

    companion object{
      /*  fun convertLiveDataListToUserInfo(users: LiveData<List<User>>): List<UserInfo>{
            val listOfUserInfo: List<UserInfo> = listOf()

           //return listOfUserInfo

            return
        }*/
    }
}